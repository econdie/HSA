<UserControl x:Class="HSA.Views.ArenaRunAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBox Text="{Binding NewHero, UpdateSourceTrigger=PropertyChanged}" />
        <TextBox Text="{Binding NewWins, UpdateSourceTrigger=PropertyChanged}" />
        <TextBox Text="{Binding NewLosses, UpdateSourceTrigger=PropertyChanged}" />
        <TextBox Text="{Binding NewDate, UpdateSourceTrigger=PropertyChanged}" />
        <Button Width="50" Height="25" Content="Save" Command="{Binding SaveCommand}"/>
        <Button Width="50" Height="25" Content="Cancel" Command="{Binding CancelCommand}"/>
        </StackPanel>
</UserControl>
